{
  "name": "@invisit/dynamodb-data-mapper-js",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "compile": "tsc -b tsconfig.json",
    "compile:watch": "tsc -b tsconfig.json -w",
    "docs": "typedoc src && lerna run docs",
    "test": "jest",
    "test:watch": "jest -w",
    "dev": "run-p -l compile:watch test:watch"
  },
  "author": {
    
    "name": "Jonathan Glanz",
    "email": "jon@invisit.io"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/jest": "^24",
    "@types/node": "^8.0.4",
    "jest": "^24",
    "lerna": "^3.13",
    "typedoc": "^0.14.0",
    "typescript": "^3.4",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "aws-sdk": "^2.7.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": ["/node_modules/", ".ts"]
  }
}
